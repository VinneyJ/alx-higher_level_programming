#!/usr/bin/python3

def square_matrix_simple(matrix=[]):
    new_matrix = matrix[:]
    moded_matrix = []
    new_moded = []
    for i in new_matrix:
        moded_matrix = list(map((lambda x : x ** 2), i))
        new_moded.append(moded_matrix)
    return new_moded
        
